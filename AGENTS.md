# CarSIte — エージェント向けの取り決め

このファイルは Codex など `AGENTS.md` を読むエージェント向け。
Claude Code 向けの `CLAUDE.md` と同じ内容を保つこと。片方だけ更新しない。


Next.js 15 (App Router) + TypeScript + Tailwind CSS。

## 現在の状態

**サブプロジェクト1「データ基盤」を実装中**（15タスク中14まで完了）。
Neon Postgres + Drizzle + Auth.js への移行はほぼ終わっている。

- 設計書: `docs/superpowers/specs/2026-08-13-data-foundation-design.md`
- 実装計画: `docs/superpowers/plans/2026-08-13-data-foundation.md`
- 全体は4つのサブプロジェクトに分割されている（データ基盤 → 収集パイプライン → 比較体験 → TCO試算）

現在のDB: models=100 / grades=103 / price_history=40 / dealers=10。
**全103グレードが `draft`** のため、公開ページは0件表示になる。これは正しい状態であり、
公開フィルタを外したり行を published にしたりして「直して」はいけない。

## 触る前に知っておくこと

- **`data/cars.json` の105件のうち62件は `scripts/generate-more-cars.js` が捏造した架空データ**である。
  価格は「基準額 + 配列の順番 × 10万円」、重量は軽900kg / それ以外一律1500kg。
  実在の諸元ではないので、これを根拠に判断してはいけない
- 車両IDに重複が2件ある（アルファードX、ステップワゴンAIR）。値も食い違っており単純な重複ではない
- 既知バグの一覧は設計書の10章「移行手順」にある。個別に直す前に設計書を読むこと

## ワークツリーの状況報告

区切りのたびに Orca のワークツリー状態を更新すること。サイドバーのカードに表示され、
ユーザーはこれを見てプロジェクトを切り替えずに進捗を把握する。

```bash
orca worktree set --worktree active --comment "<現在地と次の一手>" --json
orca worktree set --worktree active --workspace-status <todo|in-progress|in-review|completed> --json
```

**更新するタイミング**

- 調査が終わり方針が決まったとき
- 実装の区切り（フェーズ完了、テスト通過）
- **ユーザーの判断が必要になったとき（最重要）**
- 詰まって進めなくなったとき

**コメントの書き方**

- 60〜100字程度。カードで読み切れる長さにする
- 「何をしたか」ではなく「今どこにいて、次に何が起きるか」
- ユーザーの判断待ちなら冒頭に `【要確認】` を付け、`--workspace-status in-review` も設定する
- 完了報告には検証の根拠を入れる（テスト件数、通過したチェック）
- 「順調」「作業中」だけの主観的な要約は書かない

| ステータス | 意味 |
|---|---|
| `todo` | 着手前 |
| `in-progress` | 作業中 |
| `in-review` | ユーザーの確認待ちで停止中 |
| `completed` | 完了。マージ・アーカイブ可能 |

---

# ループで回せる計画書の書き方

管制塔（`~/orca/agent-coordinator`）の `loop.py` が、計画書からタスクを機械的に切り出して
ワーカーに渡す。人間はタスクごとの指示を出さない。**指示の中身は計画書に前もって書いておく。**

この規約を満たしていない計画書はループに乗らないので、人間が手で回すことになる。

規約の原本は `~/orca/agent-coordinator/LOOP_CONVENTION.md`。食い違ったら原本が正。

## 見出しの形

```
## Task <番号>: <題名>
```

- 番号は 1 から連番。飛ばさない。全角コロン `：` も可。`### Task 3: ...` でもよい
- **1つのタスク＝1つのコミット**にする。ワーカーはタスク単位で起動され、終わると消える

## タスクに必ず入れるもの

ワーカーは**そのタスクの節しか読まない**前提で書く。前後の文脈は渡らない。

```markdown
## Task 8: 公開クエリヘルパ

（何のためのタスクか。1〜3行）

**Files:**
- Create: `db/queries.ts`
- Modify: `package.json`

**Interfaces:**
- Consumes: `db`（Task 6）
- Produces: `listPublishedGrades(filters): Promise<{ rows, total }>`

- [ ] **Step 1: ...**

（実装の中身。コードを載せてよい）

- [ ] **Step 6: テストが通ることを確認**

Run: `npm run test:integration`
Expected: PASS 6 tests

- [ ] **Step 7: コミット**

（git add の対象を明示指定した git commit。`git add -A` は使わない）
```

**外せない2つ**

1. **検証コマンドと期待値** —— `Run:` と `Expected:` を必ず書く。
   検証役（Checker）はこれを実行して合否を判定する。無いとループが合否を決められない
2. **触ってよいファイルの明示** —— `Files:` に無いファイルは触らせない。
   並列で別のワーカーが動いていても衝突しないため

## ループが自動で足すもの

以下は `loop.py` が毎回先頭に注入するので、計画書に書かなくてよい。

- コミットまでは行ってよい。マージと push は絶対に行わない（人間が判断する）
- 指定されたタスクの範囲だけを触る。次のタスクに手を出さない
- 判断に迷ったら勝手に決めず、worker_done の body に明記して終える
- テストを通すために既存テストの期待値を緩めない

## 検証は別のエージェントがやる

Maker が `codex` なら Checker は `claude`、逆も同じ。**同じエージェントには絶対にしない。**
自分が書いたコードを自分で採点すると甘くなるからである。Checker には「コードは書くな。検証のみ」と指示が飛ぶ。

## ループに乗せてはいけないもの

| 条件 | 意味 |
|---|---|
| 反復性 | 同じ手順が繰り返し現れる |
| 機械的な検証可能性 | テストやビルドで合否が決まる。主観の入る余地がない |
| 経済的な価値 | ループ税（トークン消費が5〜30倍）に見合う |

**設計判断・仕様の選択・UI の見た目の評価はループに乗せない。**
これらは `【要確認】` を付けて人間に上げる。

## ループが止まる条件

止まると管制塔がワークツリーを `in-review` にし、コメント冒頭に `【要確認】` を置く。
`./status.sh` と Orca のサイドバー、スマホ通知に出る。

- 指定タスク数を終えた（正常終了。マージ判断を待つ）
- 検証が規定回数通らない（サーキットブレーカー）
- コミットが進まない周回が続く（停滞検出）
- 実時間の上限
- ワーカーが判断を求めてきた（`ask` / `escalation`）

## 投入の条件 —— ここが最重要

管制塔は次の3つが揃ったワークツリーにだけループを投げる。

1. エージェントが動いていない
2. 未コミットがゼロ
3. 計画書に残タスクがある

**作業の区切りでは必ずコミットして手を離す。** 未コミットを残したまま止まると、
ループが投入できず順番待ちになる。
